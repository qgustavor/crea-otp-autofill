{
  "name": "crea-otp-autofill",
  "version": "1.0.0",
  "private": true,
  "description": "Preenchimento automático de OTP para o portal CREANET (CREA-GO)",
  "author": "qgustavor (https://github.com/qgustavor/)",
  "scripts": {
    "build": "node build.mjs",
    "build:userscript": "node build.mjs --target=userscript",
    "build:extension": "node build.mjs --target=extension",
    "watch": "node build.mjs --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "eslint": "9",
    "neostandard": "^0.13.0"
  },
  "trustedDependencies": [
    "unrs-resolver"
  ]
}
